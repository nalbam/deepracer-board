import { Timer } from '@/components/timer/timer'

export const metadata = {
  title: 'Timer - DeepRacer Board',
  description: 'DeepRacer lap time timer with millisecond precision',
}

export default function TimerPage() {
  return <Timer />
}
